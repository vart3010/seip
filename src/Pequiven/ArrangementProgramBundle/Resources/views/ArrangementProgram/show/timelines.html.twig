{% trans_default_domain 'PequivenArrangementProgramBundle' %}
                
<div class="block">
    <div class="block-title">
        <h3>{$ goals.length $} {{ 'pequiven.goals.goals'|trans }}</h3>
        <div class="button-group absolute-right compact">
            <a class="button icon-list showPopup" href="{{ path('get_arrangementprogram_rest_restarrangementprogram_putgoalsdetails',{id:app.request.get('id'),_format:'html'}) }}" title="{{ 'pequiven.goals.goals'|trans }}">{{ 'pequiven.details'|trans({},'messages') }}</a>
        </div>
    </div>
    <div class="with-padding">
        <table class="simple-table responsive-table tables-extras" id="sorting-example2">
        <thead>
                <tr>
                    <th scope="col">{{ 'seip.table.header.name'|trans }}</th>
                    <th scope="col" width="15%" class="hide-on-mobile">{{ 'seip.table.header.type_goal'|trans }}</th>
                    <th scope="col" width="15%" class="hide-on-mobile-portrait">{{ 'seip.table.header.start_date'|trans }}</th>
                    <th scope="col" width="15%" class="hide-on-tablet">{{ 'seip.table.header.end_date'|trans }}</th>
                    <th scope="col" width="15%" class="hide-on-tablet">{{ 'seip.table.header.weight'|trans }}</th>
                    <th scope="col" width="120" class="align-right">{{ 'seip.table.header.responsible'|trans }}</th>
                </tr>
        </thead>
        <tbody ng-init="{% if entity.timeline %}goals={{ entity.timeline.goals|serialize }}{% endif %}">
            <tr ng-if="goals.length == 0">
                <td colspan="6" class="empty_row">
                    {{ "seip.table.empty.goals"|trans }}
                </td>
            </tr>
            <tr ng-repeat="goal in goals">
                <th scope="row">
                    {$ goal.name $}<br>
                    <small>{$ goal.observations $}</small>
                </th>
                <td>
                    <span ng-if="goal.typeGoal">
                        {$ goal.typeGoal.description $}
                    </span>
                    <span ng-if="!goal.typeGoal">
                        {$ 'pequiven.form.not_applicable'|trans $}
                    </span>
                </td>
                <td>{$ goal.startDate | myDate $}</td>
                <td>{$ goal.endDate| myDate $}</td>
                <td>{$ goal.weight $}</td>
                <td>{$ goal.responsible.firstName + " "+ goal.responsible.lastName $} <small class="tag">{$ " ("+goal.responsible.username+")" $}</small></td>
            </tr>
        </tbody>
    </table>
        
    </div>
</div>