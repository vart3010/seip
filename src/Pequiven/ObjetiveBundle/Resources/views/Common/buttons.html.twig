{% if is_granted('ROLE_WORKER_PLANNING') %}
    {{ buttons.generic(
        (path('pequiven_planning_update_result_objetive', { 'id': entity.id,'referral':(path('objetiveOperative_show',{id:entity.id})) })),
        ('result.recalculate'|trans({},'PequivenSEIPBundle')),
        'fa fa-pie-chart fa-1'
    ) }}
{% endif %}