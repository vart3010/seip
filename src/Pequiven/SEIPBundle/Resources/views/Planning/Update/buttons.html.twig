{% if is_granted('ROLE_WORKER_PLANNING') or is_granted('ROLE_SEIP_INDICATOR_RECALCULATE_RESULT') %}
    {{ buttons.generic(
        (path(routeUpdateResult, { 'id': entity.id,'referral':(urlUpdateResulReferral) })),
        ('result.recalculate'|trans({},'PequivenSEIPBundle')),
        'fa fa-pie-chart fa-1'
    ) }}
{% endif %}