{% trans_default_domain 'PequivenSEIPBundle' %}

{% for i in 1..31 %}
    {% set planN = "day"~ i ~ prefix ~ "Plan" %}
    {% set realN = "day" ~ i ~ prefix ~ "Real" %}
        <div class="columns">
            <div class="new-row-mobile six-columns six-columns-tablet twelve-columns-mobile">
                <p class="inline-label">
                    <label class="label">{{ 'pequiven_seip.plan_n'|trans({"%n%":i}) }}</label>
                    {{ attribute(product_detail_daily_month,planN)|myNumberFormat }}&nbsp;{{ productReport.product.productUnit }}
                </p>
            </div>
            <div class="new-row-mobile six-columns six-columns-tablet twelve-columns-mobile">
                <p class="inline-label">
                    <label class="label">{{ 'pequiven_seip.real_n'|trans({"%n%":i}) }}</label>
                    {{ attribute(product_detail_daily_month,realN)|myNumberFormat }}&nbsp;{{ productReport.product.productUnit }}
                </p>
            </div>
        </div>
{% endfor %}