{% trans_default_domain 'PequivenSEIPBundle' %}
                
<div class="block">
    <div class="block-title">
        <h3> {{ 'pequiven_seip.plant_report.plant_stop_planning'|trans }}&nbsp;({{ plant_report.plantStopPlannings|length }})</h3>
        <div class="button-group absolute-right compact">
            <a class="button icon-list-add" href="{{ path('pequiven_plant_stop_planning_create',{plantReport: plant_report.id}) }}">{{ 'pequiven.add'|trans({},'messages') }}</a>
        </div>
    </div>
    <div class="with-padding">
        <table class="simple-table responsive-table tables-extras" id="sorting-example2">
            <thead>
                <tr>
                    <th scope="col" width="35%">{{ 'Month'|trans }}</th>
                    <th scope="col" width="10%" class="hide-on-mobile-portrait">{{ 'pequiven_seip.totalStops'|trans }}</th>
                    <th scope="col" width="10%" class="hide-on-tablet">{{ 'pequiven_seip.totalHours'|trans }}</th>
                    <th scope="col" width="10%">&nbsp;</th>
                </tr>
            </thead>
            {% set colspan = 6 %}
            <tbody>

                {% for plantStopPlanning in plant_report.plantStopPlannings %}
                    <tr>
                        <td>{{ loop.index }} - 
                            {{ generateLink(plantStopPlanning) }}
                        </td>
                        <th scope="row">
                            {{ plantStopPlanning.totalStops }}&nbsp;{{ "pequiven_seip.days"|trans }}
                        </th>
                        <td>{{ plantStopPlanning.totalHours }}&nbsp;h</td>
                        <td class="low-padding align-center">
                            <a href="{{ path("pequiven_plant_stop_planning_update",{id: plantStopPlanning.id}) }}" class="button compact icon-pencil">{{ "pequiven_seip.actions.edit"|trans }}</a>
                        </td>
                    </tr>
                {% else %}
                    <tr >
                        <td colspan="{{ colspan }}" class="empty_row">
                            {{ "empty.plant_stop_planning"|trans }}
                        </td>
                    </tr>
                {% endfor %}
            </tbody>
        </table>
        
    </div>
</div>