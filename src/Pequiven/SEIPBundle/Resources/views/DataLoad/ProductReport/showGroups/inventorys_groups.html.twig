{% trans_default_domain 'PequivenSEIPBundle' %}

<div class="block">
    <div class="block-title">
        <h3> {{ 'pequiven_seip.product_report.inventory.inventorys'|trans }}&nbsp;
            ({{ inventory|length }})
        </h3>
    </div>
    <div class="with-padding">
        <table class="simple-table responsive-table tables-extras" id="sorting-example2">
            <thead>
                <tr>
                    <th scope="col" width="35%">{{ 'Month'|trans }}</th>
                    <th scope="col" width="10%" class="hide-on-mobile-portrait">{{ 'pequiven_seip.total'|trans }}</th>
                    <th scope="col" width="10%">&nbsp;</th>
                </tr>
            </thead>
            {% set colspan = 6 %}
            <tbody>
                {% for inv in inventory %}
                    <tr>
                        <td>{{ inv.month }}</td>
                        <td>{{ inv.total|myNumberFormat }}</td>
                    </tr>
                {% endfor %}
            </tbody>
        </table>

    </div>
</div>