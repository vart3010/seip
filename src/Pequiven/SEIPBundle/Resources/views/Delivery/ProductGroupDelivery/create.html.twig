{% extends 'PequivenSEIPBundle:Template:Developer/Common/formLayout.html.twig' %}

{% trans_default_domain 'PequivenSEIPBundle' %}

{% import 'PequivenSEIPBundle:Template:Developer/Macros/actions.html.twig' as actions %}

{% block before_body %}
    {{ contentHeader(
        {(path('pequiven_report_template_delivery_show',{id:app.request.get("deliveryPoint")})):("pequiven_seip.product_group_delivery.product_group"|trans )},
        ( 'pequiven.update'|trans({},'messages') )
    ) }}
{% endblock %}

{% block body -%}
    <form method="POST" action="{{ path("pequiven_product_group_delivery_create",{deliveryPoint:app.request.get("deliveryPoint"),save_and_close: true }) }}">
        <br /><br />
        {% include 'PequivenSEIPBundle:Delivery/ProductGroupDelivery:_form.html.twig' %}
        {{ actions.create() }}
        {{ form_rest(form) }}
    </form>
{% endblock %}  
