{% extends 'PequivenSEIPBundle:Template:Developer/Common/formLayoutSIG.html.twig' %}

{% block body %}
{% trans_default_domain 'PequivenSIGBundle' %}
    
    <!-- <div ng-controller="TableFormatController">    
     <div ng-controller="IndicatorSigEvolutionController">
     	<div ng-controller="EvolutionIndicatorFilters"> -->
    
        <form method="POST" id="form_action_verification">
            <fieldset class="fieldset fields-list" open="">
                <legend class="legend">{{ 'form.action_verification'|trans ({}, 'PequivenSIGBundle') }}</legend>
            
            {#<div class="field-block button-height" >
                          
                        {{ form_widget(form) }}
                        
            </div>#}
            {% for child in form %}
                    <div class="field-block button-height" >
                        {{ form_label(child) }}
                        {{ form_widget(child) }}
                        {{ form_errors(child) }}
                    </div>
            {% endfor %}

        </details> 
{% endblock %}